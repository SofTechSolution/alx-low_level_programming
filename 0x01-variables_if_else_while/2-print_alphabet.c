#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Alwayes 0 (succecc)
 */

int main(void)
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++);
{
putchar (alphabet);
}
putchar('\n');
return (0);
}
