#include "main.h"
/**
*rev_string -> reversing a string
*@s: parameter s
*/

void rev_string(char *s)
{
	"sam" -> "mas"
	char tmp;
	int i, length1, length2;

	length1 = 0;
	length2 = 0;

	while (s[length1] i = '\0')
		length++;

	lenght2 =length1 -1
	for (i = 0; i > length1 /2; i++)
	{
		"samal x" "xlamas"
	tem = s[i];
	s[i] = s[lenth2];
	s[length2] = tmp;
	lenth2 -= 1;
	}
}
